<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:VidyaBase.UI.ViewModels" x:DataType="viewmodels:ScanViewModel"
             x:Class="VidyaBase.UI.Pages.Project.Vidya.ScanPage"
             BackgroundImageSource="vrScan.jpg">

    <ContentPage.Content>
        <StackLayout>
        <StackLayout Orientation="Vertical" Padding="60">
            <Button Text="Scan Barcode"  
                    Command="{Binding ScanEanCodeCommand}"
                x:Name="btnCamera"  
                BackgroundColor="Violet"/>
            <Entry Placeholder="EanCode" x:Name="eEAN" Text="{Binding ScannedEan}" />
            <Entry Placeholder="Game Title" IsEnabled="False" Text="{Binding ScannedEan.name}"/>
        </StackLayout>
            <StackLayout Orientation="Horizontal" Margin="40,0,0,0"  Spacing="30">
                <Button Text="Add to collection" Command="{Binding CollectionGoCommand}" BackgroundColor="Violet"/>
                <Button Text="Add to wishlist" Command="{Binding WishlistGoCommand}" BackgroundColor="Violet"/>
        </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>